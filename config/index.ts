const productionURL = "https://typie-talkie-back.herokuapp.com";

const developmentURL = "http://localhost:1337";

const BASE_URL = developmentURL;

const config = { BASE_URL };
export default config;
